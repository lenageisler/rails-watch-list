<%= render "shared/navbar" %>
<div class="content-box">
    <h1>Create a new list</h1>

    <%= simple_form_for(@list, html: { class: "form" }) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Create List", class: "btn-ghost" %>
    <% end %>
</div>
